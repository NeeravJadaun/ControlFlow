# ControlFlow frontend

React + TypeScript + Vite SPA. See the [repository root README](../README.md)
for setup, demo credentials, and screenshots.

```bash
npm install
npm run dev      # http://localhost:5173, expects the API at http://localhost:8000
npm run build    # type-check + production build
npm run lint     # oxlint
```
